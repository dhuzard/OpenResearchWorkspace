# Local browser profile used by the one-shot documentation capture
.orw-playwright-github/

# Local builds and test output (not scientific workspace data)
/dist/
/build/
*.egg-info/
__pycache__/
*.py[cod]

# Throwaway workspace left by a local CLI walkthrough. Anchored to the
# repository root so a study directory of this name inside a real workspace
# is never ignored.
/my-study/
