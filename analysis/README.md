# Analysis

Use this folder for computational work that transforms data into scientific results.

- `notebooks/` — exploratory or narrative analyses.
- `scripts/` — reusable analysis scripts.
- `workflows/` — pipeline definitions and reproducible execution logic.

Keep assumptions, inputs, and environment requirements explicit. If the project evolves into reusable software, add a dedicated `src/` package rather than overloading this folder.
