# Raw data

Authoritative source data belong here only when GitHub is an appropriate storage location. Treat files here as read-only evidence: corrections should be documented and produce a new version rather than silently overwriting source data.
